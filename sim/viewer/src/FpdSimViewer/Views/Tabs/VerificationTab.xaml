<UserControl x:Class="FpdSimViewer.Views.Tabs.VerificationTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource PanelBorderStyle}">
            <StackPanel>
                <TextBlock Text="Timing Verification" Style="{StaticResource SectionTitleStyle}" />
                <ItemsControl ItemsSource="{Binding TimingChecks}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.3*" />
                                    <ColumnDefinition Width="1.2*" />
                                    <ColumnDefinition Width="1.6*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding Name}"
                                           VerticalAlignment="Center"
                                           FontWeight="SemiBold" />
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Measured}"
                                           VerticalAlignment="Center" />
                                <TextBlock Grid.Column="2"
                                           Text="{Binding Spec}"
                                           VerticalAlignment="Center"
                                           Style="{StaticResource CaptionTextStyle}" />
                                <Border Grid.Column="3"
                                        Margin="12,0,0,0"
                                        Padding="10,4"
                                        CornerRadius="10"
                                        Background="{Binding ResultBrush}">
                                    <TextBlock Text="{Binding Result}"
                                               Foreground="White"
                                               FontWeight="SemiBold" />
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource PanelBorderStyle}">
            <StackPanel>
                <TextBlock Text="Export" Style="{StaticResource SectionTitleStyle}" />
                <WrapPanel Margin="0,8,0,0">
                    <Button Content="Export CSV"
                            Command="{Binding ExportCsvCommand}"
                            Margin="0,0,8,0"
                            Padding="16,6" />
                    <Button Content="Export VCD"
                            Command="{Binding ExportVcdCommand}"
                            Padding="16,6" />
                </WrapPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource PanelBorderStyle}">
            <StackPanel>
                <TextBlock Text="Event Log" Style="{StaticResource SectionTitleStyle}" />
                <ListView ItemsSource="{Binding EventLog}" MaxHeight="320">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding TimeText}"
                                           Width="90"
                                           FontFamily="Consolas" />
                                <TextBlock Text="{Binding Description}"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
