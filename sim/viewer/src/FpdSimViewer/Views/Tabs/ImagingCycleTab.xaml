<UserControl x:Class="FpdSimViewer.Views.Tabs.ImagingCycleTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource PanelBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Imaging Timeline" Style="{StaticResource SectionTitleStyle}" />
                    <TextBlock Text="Visible Window" Style="{StaticResource CaptionTextStyle}" VerticalAlignment="Center" Margin="16,0,6,0" />
                    <Slider Width="180"
                            Minimum="100"
                            Maximum="2000"
                            TickFrequency="100"
                            IsSnapToTickEnabled="True"
                            Value="{Binding VisibleCycleWindow}" />
                    <TextBlock Text="{Binding VisibleCycleWindow}" Margin="8,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
                <ItemsControl Grid.Row="1" ItemsSource="{Binding PhaseSegments}" Margin="0,8,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="1" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="4,0"
                                    Padding="10,8"
                                    CornerRadius="8"
                                    Background="{Binding Fill}">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Opacity" Value="0.35" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                <Setter Property="Opacity" Value="1.0" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center" />
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource PanelBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="Signal Traces" Style="{StaticResource SectionTitleStyle}" />
                <ItemsControl Grid.Row="1" ItemsSource="{Binding SignalTraces}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="110" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontWeight="SemiBold" />
                                <Border Grid.Column="1" Background="#FFF7F8FA" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" CornerRadius="8" Padding="6">
                                    <Canvas Height="48">
                                        <Polyline Points="{Binding Points}" Stroke="{Binding Stroke}" StrokeThickness="2" />
                                    </Canvas>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource PanelBorderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ProgressBar Minimum="0" Maximum="100" Value="{Binding ProgressPercent}" Height="22" />
                <TextBlock Grid.Column="1" Text="{Binding ProgressText}" Margin="12,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
